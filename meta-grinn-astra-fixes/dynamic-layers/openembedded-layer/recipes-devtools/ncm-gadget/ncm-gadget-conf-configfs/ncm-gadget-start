#!/bin/sh

# NCM gadget start script - enables the gadget on the UDC

COMMON_SCRIPT=/usr/bin/ncm-gadget-common.sh

if [ ! -r "$COMMON_SCRIPT" ]; then
  echo "Error: missing helper script $COMMON_SCRIPT" >&2
  exit 1
fi

. "$COMMON_SCRIPT"
enable_gadget_udc_with_retry 10 3

exit 0
